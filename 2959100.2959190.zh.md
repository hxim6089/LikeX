# 用于 YouTube 推荐的深度神经网络

Paul Covington, Jay Adams, Emre Sargin  
Google, Mountain View, CA  
{pcovington, jka, msargin}@google.com

## 摘要

YouTube 代表了现有规模最大、最复杂的工业级推荐系统之一。本文从高层次描述该系统，并重点讨论深度学习带来的显著性能提升。论文按照信息检索中经典的两阶段划分展开：首先介绍一个深度候选生成模型，然后介绍一个独立的深度排序模型。我们还总结了在设计、迭代和维护一个具有巨大用户影响力的大规模推荐系统过程中获得的实践经验和洞见。

**关键词：** 推荐系统；深度学习；可扩展性

## 1. 引言

YouTube 是全球最大的视频内容创建、分享和发现平台。YouTube 推荐帮助超过十亿用户从不断增长的视频库中发现个性化内容。本文将重点讨论深度学习最近对 YouTube 视频推荐系统产生的巨大影响。图 1 展示了 YouTube 移动应用首页中的推荐内容。

推荐 YouTube 视频主要面临三方面的巨大挑战：

- **规模：** 许多已被证明在小规模问题上效果良好的推荐算法，在我们的规模下无法运行。为了处理 YouTube 庞大的用户群和视频库，必须使用高度专门化的分布式学习算法和高效的在线服务系统。
- **新鲜度：** YouTube 的内容库高度动态，每秒都有许多小时的视频被上传。推荐系统需要足够敏捷，既能建模新上传的内容，也能建模用户最新的行为。从探索/利用的角度看，需要在新内容和已有成熟视频之间取得平衡。
- **噪声：** 由于稀疏性以及多种不可观测的外部因素，YouTube 上的历史用户行为本质上很难预测。我们很少能获得用户满意度的真实标签，因此只能建模带噪声的隐式反馈信号。此外，与内容相关的元数据结构较差，缺乏清晰定义的本体。我们的算法必须对训练数据中的这些特性保持鲁棒。

与 Google 内部其他产品领域类似，YouTube 也经历了一个根本性的范式转变：将深度学习作为几乎所有学习问题的通用解决方案。我们的系统构建在 Google Brain [4] 之上，该系统最近以 TensorFlow [1] 的形式开源。TensorFlow 为使用大规模分布式训练实验各种深度神经网络架构提供了灵活框架。我们的模型学习约十亿个参数，并在数千亿个样本上进行训练。

与矩阵分解方法 [19] 方面的大量研究相比，使用深度神经网络构建推荐系统的工作相对较少。已有研究将神经网络用于新闻推荐 [17]、引文推荐 [8] 和评论评分预测 [20]。也有研究将协同过滤表述为深度神经网络 [22] 或自编码器 [18]。Elkahky 等人使用深度学习进行跨领域用户建模 [5]。在基于内容的场景下，Burges 等人使用深度神经网络进行音乐推荐 [21]。

本文结构如下：第 2 节给出系统概览。第 3 节更详细地描述候选生成模型，包括训练方式和如何用于在线推荐服务。实验结果表明，深层隐藏单元和额外的异构信号能够提升模型效果。第 4 节介绍排序模型，包括如何修改经典逻辑回归，使其预测期望观看时长而不是点击概率。实验结果同样表明，在该场景下增加隐藏层深度是有益的。最后，第 5 节给出结论和经验总结。

**图 1：** YouTube 移动应用首页展示的推荐内容。

## 2. 系统概览

推荐系统的整体结构如图 2 所示。系统由两个神经网络组成：一个用于候选生成，一个用于排序。

候选生成网络以用户的 YouTube 活动历史事件作为输入，从大规模视频库中取回一个较小的视频子集，通常为数百个。这些候选视频应当以较高精度与用户大体相关。候选生成网络只通过协同过滤提供粗粒度个性化。用户之间的相似性通过较粗的特征表达，例如观看过的视频 ID、搜索查询词元和人口统计信息。

要在列表中呈现少量“最佳”推荐，需要更细粒度的表示，以区分高召回候选之间的相对重要性。排序网络通过丰富的特征集合来描述视频和用户，并依据目标函数为每个视频打分。得分最高的视频会按照分数排序后展示给用户。

这种两阶段推荐方法使我们既能从非常大的视频库中生成推荐，又能确保最终出现在设备上的少量视频对用户来说是个性化且有吸引力的。此外，这种设计还支持融合其他来源生成的候选项，例如早期工作 [3] 中描述的候选来源。

在开发过程中，我们大量使用离线指标（精确率、召回率、排序损失等）来指导系统迭代改进。不过，为了最终判断某个算法或模型是否有效，我们依赖线上实验的 A/B 测试。在实时实验中，我们可以测量点击率、观看时长以及许多衡量用户参与度的指标的细微变化。这一点很重要，因为线上 A/B 结果并不总是与离线实验结果相关。

**图 2：** 推荐系统架构，展示了一个“漏斗”：候选视频先被取回并排序，最终只向用户展示少量视频。

## 3. 候选生成

在候选生成阶段，巨大的 YouTube 视频库会被缩减到数百个可能与用户相关的视频。这里描述的推荐器的前身，是一种在排序损失下训练的矩阵分解方法 [23]。我们神经网络模型的早期迭代使用浅层网络模拟了这种分解行为，只对用户过去观看的视频进行嵌入。从这个角度看，我们的方法可以视为分解技术的一种非线性泛化。

### 3.1 将推荐视为分类

我们将推荐表述为极端多分类问题：预测任务是在数百万个视频类别中，基于用户 U 和上下文 C，准确分类用户在时间 t 观看的具体视频 wt。视频库记为 V：

```text
P(wt = i | U, C) = exp(v_i u) / sum_j exp(v_j u)
```

其中，u 属于 R^N，表示用户和上下文组合的高维“嵌入”；v_j 属于 R^N，表示每个候选视频的嵌入。在这个设定中，嵌入只是把稀疏实体（单个视频、用户等）映射到 R^N 中稠密向量的方式。深度神经网络的任务，是把用户的历史和上下文学习为用户嵌入 u，使其能够通过 softmax 分类器有效地区分不同视频。

虽然 YouTube 上存在显式反馈机制（点赞/点踩、产品内调查等），但我们使用观看行为这一隐式反馈 [16] 来训练模型，即把用户完成一次视频观看作为正样本。这样选择的原因是隐式用户历史的规模比显式反馈大数个数量级，使我们能够在长尾深处生成推荐，而在这些位置显式反馈极其稀疏。

#### 高效的极端多分类

为了高效训练拥有数百万类别的模型，我们依赖一种从背景分布中采样负类的技术（“候选采样”），随后通过重要性加权校正采样偏差 [10]。对每个样本，模型会针对真实标签和采样得到的负类最小化交叉熵损失。实践中会采样数千个负类，相比传统 softmax 可获得超过 100 倍的加速。层次 softmax [15] 是一种常见替代方案，但我们无法达到相近的准确率。在层次 softmax 中，遍历树中每个节点时都要在一些往往彼此无关的类别集合之间进行区分，这会显著增加分类难度并降低性能。

在线服务时，我们需要计算最可能的 N 个类别（视频），以选择前 N 个展示给用户。在几十毫秒的严格服务延迟约束下，对数百万个项目打分需要一种相对于类别数量为次线性的近似打分方案。YouTube 之前的系统依赖哈希 [24]，本文描述的分类器也使用了类似方法。由于在线服务时并不需要 softmax 输出层给出的校准概率，打分问题可以简化为点积空间中的最近邻搜索，从而使用通用库 [12]。我们发现，A/B 测试结果对最近邻搜索算法的具体选择并不特别敏感。

### 3.2 模型架构

受连续词袋语言模型 [14] 启发，我们为固定词表中的每个视频学习高维嵌入，并将这些嵌入输入前馈神经网络。用户观看历史由一个可变长度的稀疏视频 ID 序列表示，再通过嵌入映射为稠密向量表示。网络需要固定大小的稠密输入；在几种策略（求和、按分量取最大值等）中，简单地对嵌入取平均效果最好。重要的是，这些嵌入会和其他所有模型参数一起，通过普通的梯度下降反向传播更新进行联合学习。各类特征会被拼接到宽的第一层中，随后接若干全连接的修正线性单元（ReLU）层 [6]。图 3 展示了整体网络架构，并包含下文介绍的非视频观看特征。

### 3.3 异构信号

将深度神经网络作为矩阵分解的泛化形式，一个关键优势是可以很容易地向模型中加入任意连续特征和类别特征。

搜索历史的处理方式类似观看历史：每个查询会被切分为一元词和二元词，每个词元都会被嵌入。取平均后，用户被词元化并嵌入的查询就表示为一个概括性的稠密搜索历史。

人口统计特征对于提供先验很重要，使推荐系统在新用户身上也能表现合理。用户的地理区域和设备会被嵌入并拼接。用户性别、登录状态和年龄等简单二值或连续特征，则被归一化到 [0, 1] 后作为实值直接输入网络。

#### “样本年龄”特征

YouTube 每秒都有许多小时的视频被上传。推荐这些最近上传的“新鲜”内容，对 YouTube 作为产品而言非常重要。我们持续观察到用户偏好新鲜内容，但前提是不能牺牲相关性。除了直接推荐用户想看的新视频这一一阶效果之外，还存在一个关键的二阶现象：对病毒式内容进行冷启动并传播 [11]。

机器学习系统常常表现出一种对过去的隐式偏置，因为它们是在历史样本上训练以预测未来行为。视频流行度分布高度非平稳，但我们的推荐器在视频库上产生的多项分布会反映数周训练窗口内的平均观看可能性。为校正这一点，我们在训练时把训练样本的年龄作为特征输入。在线服务时，该特征被设为 0（或略小于 0），表示模型正在训练窗口的最末端进行预测。

图 4 以任意选择的视频 [26] 为例，展示了这种方法的有效性。

**图 4：** 对于给定视频 [26]，使用样本年龄作为特征训练的模型，能够准确表示数据中观察到的上传时间以及随时间变化的流行度。如果没有该特征，模型会近似预测训练窗口内的平均观看可能性。

### 3.4 标签和上下文选择

需要强调的是，推荐常常涉及求解一个替代问题，并将其结果迁移到特定上下文中。一个经典例子是：假设准确预测评分会带来有效的电影推荐 [2]。我们发现，替代学习问题的选择对 A/B 测试性能有超出比例的巨大影响，但很难通过离线实验衡量。

训练样本来自所有 YouTube 观看行为（甚至包括嵌入到其他网站上的观看），而不仅仅来自我们推荐产生的观看。否则，新内容将很难浮现，推荐器也会过度偏向利用。如果用户通过我们的推荐之外的方式发现了视频，我们希望能够通过协同过滤迅速把这种发现传播给其他用户。另一个改善线上指标的关键洞见是：为每个用户生成固定数量的训练样本，从而在损失函数中基本等权对待用户。这避免了一小部分高度活跃用户主导损失。

有些反直觉的是，必须非常谨慎地从分类器中隐藏信息，以防模型利用网站结构并过拟合替代问题。举例来说，假设用户刚刚搜索了 “taylor swift”。由于我们的问题被设定为预测下一个观看视频，如果分类器获得该信息，它会预测最可能被观看的视频是 “taylor swift” 对应搜索结果页中的视频。毫不意外，把用户最近一次搜索页复现为首页推荐的效果非常差。通过丢弃序列信息，并用无序词袋表示搜索查询，分类器就不再直接知道标签的来源。

视频的自然消费模式通常导致非常不对称的共同观看概率。连续剧通常按顺序观看；用户发现某个音乐类型中的艺人时，也常常从最受欢迎的艺人开始，然后逐步聚焦到更小众的领域。因此我们发现，预测用户的下一次观看比预测随机留出的观看效果好得多（图 5）。许多协同过滤系统隐式地通过随机留出一个项目，并根据用户历史中的其他项目来预测它，从而选择标签和上下文（图 5a）。这种方式会泄露未来信息，并忽略任何非对称消费模式。相比之下，我们会通过选择一次随机观看来“回滚”用户历史，并且只输入用户在被留出的标签观看之前采取的行为（图 5b）。

**图 3：** 深度候选生成模型架构。嵌入后的稀疏特征与稠密特征拼接。嵌入在拼接前取平均，将可变大小的稀疏 ID 袋转换为适合输入隐藏层的固定宽度向量。所有隐藏层都是全连接层。训练时，在采样 softmax 的输出上用梯度下降最小化交叉熵损失。在线服务时，通过近似最近邻查找生成数百个候选视频推荐。

**图 5：** 为模型选择标签和输入上下文很难离线评估，但对线上性能影响很大。图中实心事件是输入网络的特征，空心事件被排除。我们发现预测未来观看（图 5b）在 A/B 测试中表现更好。在图 5b 中，样本年龄表示为 t_max - t_N，其中 t_max 是训练数据中观察到的最大时间。

### 3.5 特征和深度实验

如图 6 所示，在留出数据上，增加特征和网络深度能显著提升精确率。在这些实验中，我们使用了 100 万个视频和 100 万个搜索词元的词表，每个对象用 256 个浮点数嵌入，最多保留最近 50 次观看和最近 50 次搜索。softmax 层在相同的 100 万个视频类别上输出一个多项分布，维度为 256（可视为一个独立的输出视频嵌入）。这些模型在所有 YouTube 用户数据上训练至收敛，相当于对数据进行了多个 epoch。网络结构遵循常见的“塔形”模式：网络底部最宽，之后每一层隐藏层的单元数减半（类似图 3）。深度为 0 的网络本质上是线性分解方案，其表现与前代系统非常相似。我们不断增加宽度和深度，直到边际收益下降且收敛变得困难：

- **深度 0：** 一个线性层将拼接层转换到 256 维 softmax 空间。
- **深度 1：** 256 ReLU。
- **深度 2：** 512 ReLU -> 256 ReLU。
- **深度 3：** 1024 ReLU -> 512 ReLU -> 256 ReLU。
- **深度 4：** 2048 ReLU -> 1024 ReLU -> 512 ReLU -> 256 ReLU。

**图 6：** 视频嵌入之外的特征能够提升留出集平均准确率均值（MAP）；网络深度增加了表达能力，使模型能通过建模特征间交互来有效利用这些额外特征。

## 4. 排序

排序的主要作用是利用曝光数据，使候选预测能够针对具体用户界面进行专门化和校准。例如，一般而言用户可能很有可能观看某个视频，但由于缩略图选择的原因，对首页上的这次具体曝光不太可能点击。在排序阶段，我们可以访问更多描述视频以及用户与视频关系的特征，因为此时只需要对数百个视频打分，而不是候选生成阶段的数百万个视频。排序对于融合不同候选来源也至关重要，因为这些来源的分数不能直接比较。

我们使用与候选生成类似架构的深度神经网络，通过逻辑回归为每次视频曝光分配一个独立分数（图 7）。随后视频列表按该分数排序并返回给用户。我们的最终排序目标会根据线上 A/B 测试结果持续调整，但通常是每次曝光期望观看时长的一个简单函数。按点击率排序经常会提升那些用户并不会看完的欺骗性视频（“标题党”），而观看时长能更好地刻画用户参与度 [13, 25]。

### 4.1 特征表示

我们按照传统分类法将特征划分为类别特征和连续/有序特征。所使用的类别特征基数差异很大：有些是二值特征（例如用户是否登录），有些则有数百万种可能取值（例如用户最近一次搜索查询）。特征还会进一步按照是否只贡献单个值（“单值”）或一组值（“多值”）划分。单值类别特征的一个例子是正在打分的曝光视频 ID，而对应的多值特征可能是用户最近观看过的 N 个视频 ID 构成的袋。我们还会根据特征描述的是项目属性（“曝光”）还是用户/上下文属性（“查询”）对其分类。查询特征每次请求只计算一次，而曝光特征会针对每个被打分项目计算。

#### 特征工程

在排序模型中，我们通常使用数百个特征，类别特征与连续特征大致各占一半。尽管深度学习有望减轻手工特征工程负担，但我们的原始数据性质并不容易直接输入前馈神经网络。我们仍投入了相当多的工程资源，将用户和视频数据转换为有用特征。主要挑战在于表示用户行为的时间序列，以及这些行为如何与正在打分的视频曝光相关。

我们观察到，最重要的信号是那些描述用户过去与该项目本身及其他相似项目互动的信号，这与广告排序经验 [7] 一致。以用户过去与上传该视频的频道的历史为例：用户观看过该频道多少个视频？用户上一次观看该主题的视频是什么时候？这些描述用户过去在相关项目上行为的连续特征尤其强大，因为它们能很好地泛化到不同项目之间。我们还发现，将候选生成阶段的信息以特征形式传递给排序阶段非常关键，例如：哪个来源提名了这个候选视频？这些来源给出了什么分数？

描述过去视频曝光频率的特征，对于在推荐中引入“流动性”也非常关键，也就是说连续请求不应返回完全相同的列表。如果用户最近被推荐了一个视频但没有观看，模型自然会在下一次页面加载时降低该曝光的排序。提供精确到秒级的曝光和观看历史本身就是一项工程壮举，超出了本文范围，但它对产生响应迅速的推荐至关重要。

#### 嵌入类别特征

与候选生成类似，我们使用嵌入把稀疏类别特征映射为适合神经网络的稠密表示。每个唯一 ID 空间（“词表”）都有一个独立学习的嵌入，其维度大致按唯一取值数量的对数增长。这些词表是简单的查找表，在训练前通过对数据进行一次遍历构建。对于基数非常大的 ID 空间（例如视频 ID 或搜索查询词），会根据其在被点击曝光中的频率排序，只保留前 N 个。词表外取值会简单映射到零嵌入。与候选生成一样，多值类别特征的嵌入在输入网络前会取平均。

重要的是，同一 ID 空间中的类别特征也会共享底层嵌入。例如，存在一个全局视频 ID 嵌入，被许多不同特征使用：曝光视频 ID、用户最近观看的视频 ID、触发该推荐的“种子”视频 ID 等。尽管共享嵌入，每个特征仍会分别输入网络，以便上层网络能够为每个特征学习专门化表示。共享嵌入对于提升泛化能力、加快训练并减少内存需求很重要。模型参数的绝大多数都位于这些高基数嵌入空间中。例如，100 万个 ID 嵌入到 32 维空间，其参数量比宽度为 2048 的全连接层多 7 倍。

#### 归一化连续特征

众所周知，神经网络对输入的尺度和分布非常敏感 [9]，而决策树集成等替代方法则对单个特征的缩放不敏感。我们发现，对连续特征进行适当归一化对收敛至关重要。给定一个分布为 f 的连续特征 x，我们通过累计分布对取值进行缩放，将其变换为 x_bar，使该特征在 [0, 1) 上近似均匀分布。该积分通过对训练前一次遍历数据计算得到的特征分位数进行线性插值来近似。

除了原始归一化特征 x_bar，我们还输入 x_bar^2 和 sqrt(x_bar)，让网络更容易形成该特征的超线性和次线性函数，从而获得更强表达能力。输入连续特征的幂次被发现可以提升离线准确率。

**图 7：** 深度排序网络架构。图中展示了类别特征（单值和多值）的嵌入、共享嵌入以及归一化连续特征的幂次。所有层均为全连接层。实践中，数百个特征会被输入网络。

### 4.2 建模期望观看时长

我们的目标是在训练样本为正例（视频曝光被点击）或负例（曝光未被点击）的条件下，预测期望观看时长。正例样本会标注用户花在该视频上的观看时间。为了预测期望观看时长，我们使用了加权逻辑回归技术，这一技术正是为此目的而开发的。

模型在交叉熵损失下用逻辑回归训练（图 7）。不过，正例（被点击）曝光会按该视频上观察到的观看时长加权。负例（未点击）曝光全部赋予单位权重。这样，逻辑回归学习到的 odds 为：

```text
sum_i T_i / (N - k)
```

其中 N 是训练样本数量，k 是正例曝光数量，T_i 是第 i 次曝光的观看时长。假设正例曝光比例很小（在我们的场景中确实如此），学习到的 odds 约为 E[T](1 + P)，其中 P 是点击概率，E[T] 是该曝光的期望观看时长。由于 P 很小，这个乘积接近 E[T]。在推理时，我们使用指数函数 exp(x) 作为最终激活函数，以产生这些近似期望观看时长的 odds。

### 4.3 隐藏层实验

表 1 展示了不同隐藏层配置在次日留出数据上的结果。每个配置显示的数值（“加权的逐用户损失”）通过同时考虑用户在单个页面上看到的正例（被点击）和负例（未点击）曝光得到。我们首先用模型为这两个曝光打分。如果负例曝光得分高于正例曝光，我们就把正例曝光的观看时长视为预测错误的观看时长。加权的逐用户损失即为预测错误的观看时长总量占留出曝光对总观看时长的比例。

这些结果表明，增大隐藏层宽度可以改善效果，增加深度也同样有效。不过，代价是推理所需的服务器 CPU 时间增加。1024 宽 ReLU 后接 512 宽 ReLU，再接 256 宽 ReLU 的配置，在让我们保持在在线服务 CPU 预算内的同时取得了最佳结果。

对于 1024 -> 512 -> 256 模型，我们曾尝试只输入归一化连续特征而不输入其幂次，结果损失增加了 0.2%。在相同隐藏层配置下，我们还训练了一个正负样本等权的模型。不出所料，这使观看时长加权损失大幅增加了 4.1%。

| 隐藏层 | 加权的逐用户损失 |
|---|---:|
| 无 | 41.6% |
| 256 ReLU | 36.9% |
| 512 ReLU | 36.7% |
| 1024 ReLU | 35.8% |
| 512 ReLU -> 256 ReLU | 35.2% |
| 1024 ReLU -> 512 ReLU | 34.7% |
| 1024 ReLU -> 512 ReLU -> 256 ReLU | 34.6% |

**表 1：** 更宽、更深的 ReLU 隐藏层，对次日留出数据上按观看时长加权的成对损失的影响。

## 5. 结论

本文描述了用于 YouTube 视频推荐的深度神经网络架构，并将问题拆分为两个不同任务：候选生成和排序。

我们的深度协同过滤模型能够有效吸收多种信号，并通过深层网络建模它们之间的交互，优于 YouTube 之前使用的矩阵分解方法 [23]。为推荐选择替代问题时，艺术成分多于科学成分；我们发现，对未来观看进行分类能捕捉非对称的共同观看行为并避免未来信息泄露，因此在线上指标上表现良好。从分类器中隐藏具有区分性的信号同样是取得好结果的关键；否则模型会过拟合替代问题，难以很好迁移到首页推荐。

我们证明，将训练样本年龄作为输入特征，可以消除模型固有的过去偏置，并使模型表示热门视频随时间变化的行为。这提升了离线留出集精确率，并在 A/B 测试中显著增加了最近上传视频的观看时长。

排序是一个更经典的机器学习问题，但我们的深度学习方法在观看时长预测上优于先前的线性方法和基于树的方法。推荐系统尤其受益于描述用户过去与项目互动行为的专门特征。深度神经网络需要对类别特征和连续特征进行特殊表示，我们分别使用嵌入和分位数归一化来转换它们。实验表明，网络深度能够有效建模数百个特征之间的非线性交互。

我们通过对正样本按观看时长加权、对负样本赋予单位权重的方式修改了逻辑回归，使其能够学习接近期望观看时长的 odds。与直接预测点击率相比，这种方法在按观看时长加权的排序评估指标上表现好得多。

## 6. 致谢

作者感谢 Jim McFadden 和 Pranav Khaitan 提供的宝贵指导与支持。Sujeet Bansal、Shripad Thite 和 Radek Vingralek 实现了训练与在线服务基础设施中的关键组件。Chris Berg 和 Trevor Walker 贡献了深入讨论和详细反馈。

## 7. 参考文献

参考文献保留原文作者与出处，便于检索：

[1] M. Abadi 等. TensorFlow: Large-scale machine learning on heterogeneous systems, 2015.  
[2] X. Amatriain. Building industrial-scale real-world recommender systems. RecSys 2012.  
[3] J. Davidson 等. The YouTube video recommendation system. RecSys 2010.  
[4] J. Dean 等. Large scale distributed deep networks. NIPS 2012.  
[5] A. M. Elkahky, Y. Song, X. He. A multi-view deep learning approach for cross domain user modeling in recommendation systems. WWW 2015.  
[6] X. Glorot, A. Bordes, Y. Bengio. Deep sparse rectifier neural networks. AISTATS 2011.  
[7] X. He 等. Practical lessons from predicting clicks on ads at Facebook. ADKDD 2014.  
[8] W. Huang 等. A neural probabilistic model for context based citation recommendation. AAAI 2015.  
[9] S. Ioffe, C. Szegedy. Batch normalization: Accelerating deep network training by reducing internal covariate shift. 2015.  
[10] S. Jean 等. On using very large target vocabulary for neural machine translation. 2014.  
[11] L. Jiang 等. Viral video style: A closer look at viral videos on YouTube. ICMR 2014.  
[12] T. Liu 等. An investigation of practical approximate nearest neighbor algorithms. 2004.  
[13] E. Meyerson. YouTube Now: Why we focus on watch time. 2012.  
[14] T. Mikolov 等. Distributed representations of words and phrases and their compositionality. 2013.  
[15] F. Morin, Y. Bengio. Hierarchical probabilistic neural network language model. AISTATS 2005.  
[16] D. Oard, J. Kim. Implicit feedback for recommender systems. 1998.  
[17] K. J. Oh 等. Personalized news recommendation using classified keywords to capture user preference. 2014.  
[18] S. Sedhain 等. AutoRec: Autoencoders meet collaborative filtering. WWW Companion 2015.  
[19] X. Su, T. M. Khoshgoftaar. A survey of collaborative filtering techniques. 2009.  
[20] D. Tang 等. User modeling with neural network for review rating prediction. IJCAI 2015.  
[21] A. van den Oord, S. Dieleman, B. Schrauwen. Deep content-based music recommendation. NIPS 2013.  
[22] H. Wang, N. Wang, D.-Y. Yeung. Collaborative deep learning for recommender systems. KDD 2015.  
[23] J. Weston, S. Bengio, N. Usunier. WSABIE: Scaling up to large vocabulary image annotation. IJCAI 2011.  
[24] J. Weston, A. Makadia, H. Yee. Label partitioning for sublinear ranking. ICML 2013.  
[25] X. Yi 等. Beyond clicks: Dwell time for personalization. RecSys 2014.  
[26] Zayn. Pillowtalk.

