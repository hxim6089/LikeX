# Required: the backend will not start without these values.
DB_PASSWORD=replace-with-your-mysql-password
JWT_SECRET=replace-with-at-least-32-random-characters

# Optional: the related feature degrades gracefully when left empty.
DEEPSEEK_API_KEY=
KAGGLE_API_KEY=
