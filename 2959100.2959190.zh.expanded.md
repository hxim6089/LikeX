# 基于深度神经网络的 YouTube 推荐系统

Paul Covington, Jay Adams, Emre Sargin  
Google, Mountain View, CA  
{pcovington, jka, msargin}@google.com

## 摘要

YouTube 是目前世界范围内规模最大、工程复杂度最高、用户影响最广的工业级推荐系统之一。本文从整体架构出发，对 YouTube 视频推荐系统进行概括性说明，并重点讨论深度学习方法在该系统中带来的显著性能提升。论文按照信息检索系统中常见的两阶段框架展开：第一阶段是候选生成，即从海量视频库中快速召回一小批可能与用户相关的视频；第二阶段是排序，即利用更丰富的用户、视频和上下文特征，对候选视频进行精细化打分并决定最终展示顺序。本文分别介绍了一个深度候选生成模型和一个独立的深度排序模型，并结合大规模线上系统的实践，说明模型设计、特征处理、训练目标选择、在线服务效率以及 A/B 测试之间的关系。作者还总结了在设计、迭代和维护一个对用户体验具有巨大影响的推荐系统过程中获得的工程经验和方法启示。

**关键词：** 推荐系统；深度学习；候选生成；排序；可扩展性

## 1. 引言

YouTube 是全球最大的视频内容创建、分享与发现平台。对用户而言，平台上的视频数量极其庞大，而且每天都在持续增长，单纯依靠搜索或人工浏览很难高效发现感兴趣的内容。因此，推荐系统成为 YouTube 产品体验中的核心组成部分。YouTube 推荐负责帮助超过十亿用户从不断扩张的视频集合中发现个性化内容，并在首页、观看页以及其他入口中持续影响用户的视频消费路径。本文关注的是深度学习近年来对 YouTube 视频推荐系统产生的深刻影响。原文图 1 展示了 YouTube 移动应用首页中的推荐内容，它体现了推荐系统最终面向用户的呈现形式：系统需要在有限屏幕空间内选择少量视频，并尽可能保证这些视频与用户兴趣相匹配。

从产品角度看，YouTube 推荐不仅是一个算法排序问题，也是一个连接用户、内容创作者和平台生态的发现机制。用户来到平台时，往往并没有明确查询词，或者只具有模糊兴趣；推荐系统需要根据用户过去的观看、搜索、设备、地区和当前上下文，主动判断哪些内容可能值得展示。与此同时，视频平台内容集合具有明显长尾特征，大量视频并非全局热门，却可能对特定用户非常有价值。一个有效的推荐系统需要在热门内容、个性化长尾内容和新上传内容之间保持平衡。

对 YouTube 进行视频推荐具有非常明显的挑战性。第一项挑战是规模。许多在小规模数据集上已经被证明有效的推荐算法，一旦放到 YouTube 的真实业务规模中，就会遇到训练、存储和在线计算上的瓶颈。YouTube 同时拥有极其庞大的用户群体和视频语料库，推荐系统不仅需要学习海量用户行为，还必须在严格延迟约束下完成在线服务。因此，系统需要高度专业化的分布式学习算法，也需要能够高效响应请求的服务架构。模型效果固然重要，但如果不能在生产环境中稳定、低延迟地运行，就无法真正改善用户体验。

第二项挑战是新鲜度。YouTube 的视频库高度动态，每秒都有大量新视频被上传。推荐系统不仅要理解已有视频和长期稳定的用户兴趣，还要及时响应新上传内容以及用户近期行为的变化。对于视频平台来说，新内容的发现和传播非常重要。如果推荐系统过于依赖历史统计，就可能长期偏向已有热门视频，导致新视频难以获得曝光；但如果过度推荐新内容，又可能牺牲推荐相关性。这个问题可以从探索与利用的角度理解：系统既要利用已经掌握的用户兴趣，又要保留探索新内容的能力。

新鲜度问题还关系到内容生态的健康。对创作者而言，新上传视频如果长期无法获得初始曝光，就很难积累观看、互动和后续传播；对用户而言，如果推荐结果总是停留在过去已经流行的内容上，平台会显得缺乏活力。YouTube 推荐系统需要在用户满意度和内容发现之间做动态权衡：既不能把低相关的新视频强行推给用户，也不能让历史热门内容永久占据推荐位。深度模型通过样本年龄、近期行为和内容特征共同建模，为这种权衡提供了可学习的方式。

第三项挑战是噪声。YouTube 上的用户行为具有稀疏性和不确定性，用户观看某个视频可能受到时间、地点、设备、社交传播、外部热点等大量不可观测因素影响。系统很少能够直接获得用户满意度的真实标签，更多时候只能使用观看、点击、停留时长等隐式反馈信号。这些信号虽然规模庞大，但并不完全等价于用户偏好。例如，用户点击了某个视频并不意味着真正满意，用户没有点击某个视频也不一定说明不感兴趣。此外，视频内容的元数据往往结构松散，缺乏严格定义的本体体系。推荐算法必须能够在这种带噪、稀疏且动态变化的数据环境中保持鲁棒。

在 Google 内部其他产品领域的影响下，YouTube 也逐步完成了从传统机器学习方法向深度学习方法的范式转变。深度学习被用作许多学习问题的通用解决方案，原因在于它能够从大规模数据中自动学习复杂的非线性表示，并且能够融合多种类型的输入特征。本文所述系统构建在 Google Brain 相关基础设施之上，而该基础设施后来以 TensorFlow 的形式开源。TensorFlow 为大规模分布式训练、灵活实验不同神经网络结构以及快速迭代模型提供了重要支持。作者指出，他们的模型大约学习十亿级别的参数，并在数千亿个训练样本上进行训练，这说明本文讨论的并不是实验室规模的推荐任务，而是真实工业系统中的超大规模学习问题。

与矩阵分解方法相关的大量研究相比，将深度神经网络直接用于推荐系统的研究在当时仍然相对较少。已有工作曾将神经网络用于新闻推荐、引文推荐、评分预测、协同过滤和自编码器建模，也有研究将深度学习用于跨领域用户建模和基于内容的音乐推荐。这些工作说明深度学习在推荐任务中具有潜力，但 YouTube 的场景在规模、新鲜度、噪声和在线服务约束方面都更加复杂。本文的贡献并不是提出一个单独的学术模型，而是展示深度神经网络如何在大规模工业推荐系统中与候选生成、排序、特征工程、在线检索和 A/B 测试结合起来。

在推荐系统研究中，模型离线表现与线上体验之间经常存在差距。YouTube 场景尤其如此，因为用户看到的是有限数量的视频卡片，而不是完整候选集合；用户是否点击、是否继续观看、是否对推荐感到满意，还受到缩略图、标题、页面位置、近期兴趣变化和其他外部因素影响。因此，本文虽然讨论深度神经网络结构，但始终把模型放在完整系统中理解。候选生成、排序、特征工程、目标函数和在线实验共同构成推荐系统的实际效果。

全文结构如下。第 2 节给出推荐系统的整体概览，说明候选生成和排序两个阶段如何组成完整推荐管线。第 3 节详细介绍候选生成模型，包括如何将推荐问题转化为极端多分类问题，如何使用采样 softmax 进行高效训练，如何融合观看历史、搜索历史、人口统计特征和样本年龄等异构信号，以及如何选择训练标签和上下文。第 4 节介绍排序模型，重点讨论类别特征和连续特征的表示方法、特征工程的重要性，以及如何通过加权逻辑回归预测期望观看时长。第 5 节总结论文结论和系统实践经验。

**图 1：** YouTube 移动应用首页显示的推荐内容。

## 2. 系统概述

YouTube 推荐系统的整体结构可以概括为一个两阶段架构，如原文图 2 所示。第一阶段是候选生成网络，第二阶段是排序网络。候选生成网络负责从数百万规模的视频库中快速取回几百个可能与用户相关的视频；排序网络则对这些候选视频进行更细粒度的评分，最终只选择少量视频展示给用户。这样的两阶段设计既满足了大规模召回的效率要求，也保留了最终排序时使用复杂特征和精细目标函数的空间。

候选生成网络的输入主要来自用户在 YouTube 上的历史活动，例如曾经观看过的视频、搜索查询、上下文信息和人口统计特征。该网络的目标不是直接决定最终展示列表，而是以较高召回率和较好精度缩小搜索空间。候选生成阶段提供的是较粗粒度的个性化，其核心思想接近协同过滤：如果两个用户在观看历史、搜索行为或人口统计信息上存在相似性，那么他们可能对某些视频具有相似兴趣。这里的用户相似性不是通过人工定义的规则表达，而是通过神经网络学习得到的稠密向量表示表达。

这种候选生成方式可以理解为把用户映射到一个兴趣空间，再在同一空间中寻找与用户向量接近的视频。与显式构造用户画像不同，深度模型学习到的用户向量并不直接对应某个可读标签，而是由大量历史行为和上下文共同决定。它既可以反映用户长期兴趣，也可以在输入近期行为后反映短期兴趣变化。由于候选生成阶段面对的是全量视频库，模型和服务系统必须共同保证召回速度，否则再好的候选质量也无法在真实产品中使用。

排序阶段则承担更精细的决策任务。当候选视频已经缩小到几百个以后，系统可以为每个候选视频计算更多特征，包括视频本身的属性、用户与该视频或相似视频的历史交互、候选来源分数、最近曝光历史、设备和上下文等。排序网络根据目标函数为每个候选视频分配分数，然后按照分数排序，向用户展示排名靠前的视频。由于排序阶段面对的候选数量有限，它可以使用比候选生成阶段更丰富、更昂贵的特征，从而更准确地刻画用户在当前界面下点击和观看某个视频的可能性。

采用两阶段推荐管线的一个关键优势是可扩展性。系统可以先用候选生成阶段处理数百万视频库，再用排序阶段处理几百个候选，这样既避免对所有视频进行复杂打分，又能保证最终推荐结果具有较强个性化。另一个优势是可组合性。候选视频不一定全部来自深度候选生成模型，也可以融合来自其他候选源的结果，例如基于订阅、热门趋势、相似视频或其他业务逻辑产生的候选。排序网络可以在统一框架下对不同候选源进行校准和融合。

这种结构也体现了信息检索系统中的“漏斗”思想：越靠前的阶段处理对象越多，因此模型必须轻量、快速、覆盖面广；越靠后的阶段处理对象越少，因此可以使用更复杂的特征和更精细的目标函数。YouTube 推荐系统把这一思想应用到视频推荐中，使候选生成阶段承担大规模检索职责，排序阶段承担最终决策职责。对于其他推荐场景，例如新闻、商品或社交内容推荐，这种漏斗式架构同样具有借鉴意义。

在系统开发过程中，离线指标仍然具有重要作用。研究人员会使用精确率、召回率、排序损失等指标指导模型迭代，快速判断某些特征、网络结构或训练目标是否可能有效。然而，YouTube 的最终模型选择依赖线上 A/B 测试。原因在于离线指标与真实用户体验之间并不总是高度一致。一个模型在离线留出集上表现更好，不一定能提高用户在线点击率、观看时长或满意度。线上实验能够测量用户实际行为的细微变化，因此是判断算法是否真正有效的最终依据。

两阶段框架还降低了系统演化成本。候选生成模型可以专注于大规模召回和粗粒度个性化，排序模型则专注于最终展示位置的精确优化。二者可以相对独立地迭代：当候选生成模型改进时，排序模型可以继续融合新候选；当排序目标调整时，也不必完全重写召回系统。这种模块化设计使大规模推荐系统能够在持续服务用户的同时不断更新。

**图 2：** 推荐系统架构：候选视频经由候选生成和排序后，仅向用户展示少量视频。

## 3. 候选生成

候选生成阶段的任务是从庞大的 YouTube 视频语料库中筛选出数百个可能与当前用户相关的视频。该阶段面对的是极端大规模的检索问题。如果直接对所有视频进行精细排序，计算代价无法接受；因此，候选生成模型必须在保证较高召回率的同时满足严格的在线延迟约束。本文描述的候选生成方法可以看作传统矩阵分解方法的非线性扩展。早期系统曾使用在排序损失下训练的矩阵分解模型，而神经网络模型的早期版本也通过浅层网络嵌入用户历史观看记录来模拟类似分解行为。随着深度网络结构和更多特征的引入，模型能够表达比线性分解更复杂的用户兴趣模式。

### 3.1 推荐即分类

本文将推荐问题表述为极端多分类问题。直观地说，给定用户 U 及其上下文 C，模型要从数百万个候选视频类别中预测用户在时间 t 会观看哪一个视频。视频库记为 V，用户在时间 t 观看的视频记为 wt，则模型需要估计如下概率：

```text
P(wt = i | U, C) = exp(v_i u) / sum_j exp(v_j u)
```

其中，u 属于 R^N，表示用户与上下文组合后的高维嵌入向量；v_j 属于 R^N，表示第 j 个候选视频的嵌入向量。在这个设定中，所谓嵌入就是把稀疏实体映射到低维或中高维稠密向量空间中。视频 ID、用户行为、查询词、设备和地域等原始输入通常非常稀疏，无法直接用于普通神经网络。通过嵌入层，模型可以把这些离散对象转化为连续向量，并在训练中学习它们之间的相似性和交互关系。

深度神经网络在这里的任务，是把用户历史和上下文映射为一个用户向量 u，使得该向量在点积空间中更接近用户可能观看的视频向量，而远离不太可能观看的视频向量。softmax 输出层提供了一个多类别分类目标，使模型能够通过反向传播学习用户表示和视频表示。与传统协同过滤相比，这种方式不仅能够利用观看历史，还能自然融合搜索历史、人口统计特征、设备信息和时间相关特征。

这里的分类问题之所以称为“极端多分类”，是因为类别数量不是几十或几百，而是数百万级别的视频。每一个视频都可以被看作一个类别，用户下一次观看的视频就是分类标签。这样的建模方式把推荐任务转换成监督学习问题，使系统能够利用成熟的神经网络训练方法。但它也带来两个难点：训练时完整 softmax 成本过高，在线服务时也无法逐一计算所有类别。因此，后续的候选采样和近似最近邻检索是该建模方式能够落地的关键。

YouTube 虽然存在点赞、点踩、问卷调查等显式反馈机制，但本文的候选生成模型主要使用观看行为这一隐式反馈进行训练。也就是说，用户完成一次视频观看会被视为正样本。选择隐式反馈的原因在于其规模远远大于显式反馈。对于长尾视频而言，显式评分或点赞可能极其稀疏，无法支撑有效学习；而观看记录覆盖范围更广，可以让系统在视频库深处也生成有意义的推荐。当然，隐式反馈也带来噪声问题，因此后续排序阶段还需要更精细地建模观看时长和用户参与度。

#### 高效的极端多分类

在数百万类别上训练 softmax 分类器非常昂贵。若对每个训练样本都计算所有类别的归一化概率，训练成本会极高。为解决这一问题，作者采用候选采样技术，从背景分布中采样一部分负类，并通过重要性加权对采样偏差进行校正。对每个样本，训练过程只在真实标签和采样得到的负类上最小化交叉熵损失。实践中，每个样本会采样数千个负类，相比完整 softmax 能够获得超过百倍的训练加速。

候选采样的思想是承认绝大多数负类在单个训练样本中并不需要被完整枚举。模型只需在真实观看视频和若干代表性负样本之间学习区分能力，就可以逐步形成有效的用户和视频表示。重要性加权则用于修正采样分布与真实类别分布之间的差异，避免模型因为采样方式而学习到偏置概率。对于 YouTube 这种类别空间极大的任务，这类近似训练方法不是可选优化，而是系统能够训练起来的必要条件。

层次 softmax 是另一种常见替代方法，它通过树结构把多分类问题分解成一系列节点决策。然而，作者发现层次 softmax 在该任务上无法达到相近的准确率。原因在于，树中每个节点往往需要在一组并不自然相关的类别集合之间进行判别，这会使分类问题变得更困难，并降低模型对视频相似性的刻画能力。相比之下，候选采样配合普通 softmax 目标更适合 YouTube 的视频推荐场景。

训练完成后，在线服务阶段需要从数百万视频中找到得分最高的 N 个视频。如果逐一计算所有视频的 softmax 概率，同样无法满足几十毫秒级别的延迟要求。由于在线服务时并不需要校准后的类别概率，只需要找到最可能的视频，问题可以转化为点积空间中的近邻搜索。系统只需根据用户向量 u 查找与其点积最高的视频向量。此前 YouTube 系统曾使用哈希方法，本文模型也采用类似的近似检索思想，并可以使用通用近似最近邻库实现。作者还指出，线上 A/B 测试结果对具体近邻搜索算法的选择并不特别敏感，说明召回质量更多取决于模型表示本身。

### 3.2 模型架构

候选生成模型受到连续词袋语言模型的启发。模型为固定词表中的每个视频学习高维嵌入，并将用户历史中的视频嵌入组合后输入前馈神经网络。用户观看历史本质上是一个长度可变的视频 ID 序列，而神经网络通常需要固定长度的稠密输入。为解决这一问题，系统会把用户最近观看过的视频 ID 映射为嵌入向量，然后对这些向量进行聚合。作者比较了求和、按维度取最大值和取平均等策略，发现简单平均在实践中效果最好。

平均嵌入的做法有两个优点。第一，它能够把任意长度的观看历史转换为固定宽度向量，方便输入全连接网络。第二，它避免模型过度依赖序列中的单个视频，使用户表示更像近期兴趣的整体摘要。需要注意的是，这些视频嵌入并不是预先固定的，而是与模型其他参数一起通过梯度下降和反向传播联合学习。也就是说，模型会根据推荐目标自动调整视频向量，使其适合区分用户可能观看的视频。

除观看历史外，模型还会把多种特征拼接到较宽的第一层中，然后经过多层全连接 ReLU 隐藏层。ReLU 层能够引入非线性，使模型不再局限于线性协同过滤。随着网络深度增加，模型能够刻画观看历史、搜索历史、人口统计特征、设备和时间特征之间更复杂的交互关系。原文图 3 展示了深度候选生成模型的整体架构，其中稀疏特征先通过嵌入层转换为稠密向量，再与连续特征拼接，最终输入隐藏层并在训练时通过采样 softmax 优化。

从结构上看，候选生成网络的底部负责把不同来源的原始信号转换成可学习的向量表示，中间隐藏层负责组合这些信号并学习非线性关系，顶部 softmax 层则负责把用户表示与视频类别联系起来。训练阶段需要 softmax 或采样 softmax 来提供分类损失；在线服务阶段则不再需要完整概率分布，而是把网络输出的用户向量交给近似最近邻检索系统，用于快速召回候选视频。这种训练与服务阶段的分工，是论文中非常典型的工业系统设计。

**图 3：** 深度候选生成模型架构。

### 3.3 多样化信号

深度神经网络相较于传统矩阵分解的一个重要优势，是能够自然接收异构特征。传统矩阵分解通常主要依赖用户与物品交互矩阵，而神经网络可以同时处理类别特征、连续特征、二值特征和上下文特征。对于 YouTube 这样的平台，仅使用观看历史并不足以刻画用户兴趣，因为用户可能通过搜索、浏览、设备切换或地域变化表现出不同需求。

搜索历史的处理方式与观看历史类似。系统会把用户查询切分为一元词和二元词，然后为每个查询词元学习嵌入。对这些词元嵌入取平均后，就可以得到用户搜索兴趣的稠密表示。搜索历史对于推荐非常重要，因为它往往表达了用户近期主动提出的需求；而观看历史则更多反映用户已经消费过的内容。二者结合可以让模型同时理解长期偏好和即时兴趣。

例如，一个用户长期观看游戏视频，但最近连续搜索某位歌手或某个体育赛事，那么搜索历史可能提供比长期观看历史更及时的兴趣信号。相反，某次搜索也可能只是临时需求，不能完全覆盖用户长期偏好。因此，深度模型需要在多种信号之间自动学习权重和交互，而不是依靠人工规则简单决定“搜索更重要”或“观看更重要”。这正是深度神经网络在异构信号融合中的优势。

人口统计特征在新用户场景中尤其重要。当用户历史行为很少时，模型难以仅凭观看记录推断偏好，此时地域、设备、年龄、性别和登录状态等特征可以提供先验信息。例如，不同地区用户可能对语言、文化或热门事件有不同偏好，不同设备上的观看场景也可能不同。模型会把地域和设备等类别特征嵌入后拼接到网络中，而性别、登录状态、年龄等二值或连续特征则被归一化到 [0,1] 后直接作为实值输入。

#### 样本年龄特征

YouTube 每秒都有大量视频上传，推荐新鲜内容对平台至关重要。用户通常偏好新内容，但这种偏好不能以牺牲相关性为代价。推荐系统需要在新鲜度和匹配度之间进行平衡。如果系统只根据过去几周的平均观看概率进行预测，它会自然偏向训练窗口中已经积累大量观看的旧视频，从而对新上传视频不利。这种现象可以理解为机器学习系统对历史数据的隐式偏置。

视频流行度分布高度非平稳。一个视频可能在上传后短时间内迅速流行，也可能随着热点消退而降低热度。若模型只学习训练窗口内的平均观看概率，就无法准确表示这种随时间变化的动态。为校正这一问题，作者在训练时把样本年龄作为输入特征。样本年龄表示训练样本距离训练窗口末端的时间差。在线服务时，该特征被设为零或略小于零，表示模型正在训练窗口最末端对未来进行预测。

这个设计的直观含义是：训练样本不仅告诉模型“某个视频被观看了”，还告诉模型“这个观看行为发生在视频生命周期的哪个时间位置”。模型因此能够区分上传初期的爆发、稳定期的持续观看以及热度衰减。在线预测时把样本年龄设为训练窗口末端，相当于让模型站在当前时间点判断视频是否值得推荐。对于不断产生新内容的平台，这种时间建模比静态流行度估计更符合真实内容生态。

加入样本年龄特征后，模型能够学习“刚上传的视频在当前时刻更可能被观看”这类时间相关模式。原文图 4 以任意选择的视频为例，展示了该方法的有效性。没有样本年龄特征时，模型倾向于预测训练窗口内的平均观看概率；加入该特征后，模型能够更准确地表示视频上传时间以及随时间变化的流行度。这不仅改善了离线留出集表现，也能在线上 A/B 测试中增加近期上传视频的观看时长。

**图 4：** 加入样本年龄特征后，模型能够表示视频上传时间与随时间变化的流行度。

### 3.4 标签与上下文选择

推荐系统常常并不是直接优化最终业务目标，而是先构造一个可训练的代理问题，再把学习结果迁移到具体推荐场景中。选择什么样的代理问题，对最终线上表现具有很大影响。一个经典例子是电影推荐中的评分预测：准确预测评分并不一定等价于生成最有效的推荐。YouTube 的经验同样表明，训练标签和输入上下文的选择会显著影响 A/B 测试结果，但这种影响很难仅通过离线实验完全评估。

候选生成模型的训练样本来自所有 YouTube 观看行为，而不仅限于推荐系统已经产生的观看。这一点很重要。如果只使用推荐带来的观看作为训练数据，模型会不断强化已有推荐路径，导致新内容更难被发现，也会使系统过度偏向利用已有热门内容。相反，如果用户通过搜索、外部嵌入页面或其他方式发现了某个视频，系统也应当利用这种发现，并通过协同过滤把它传播给可能感兴趣的其他用户。

这也体现了推荐系统中的反馈循环问题。推荐系统影响用户看到什么，用户行为又反过来成为训练数据。如果训练数据只来自系统自己过去推荐的内容，模型会越来越受过去决策限制，难以发现推荐之外产生的新兴趣和新内容。把所有观看行为纳入训练，可以在一定程度上减弱这种闭环偏置，让外部发现路径也能进入推荐模型。

另一个关键策略是为每个用户生成固定数量的训练样本。这样做相当于在损失函数中让用户获得近似相等的权重，避免少数极度活跃用户主导训练过程。如果不进行这种平衡，模型可能过度拟合高活跃用户的行为模式，而对普通用户的推荐效果下降。对于拥有海量用户的平台来说，如何在训练目标中平衡不同用户群体，是影响最终体验的重要因素。

有些反直觉的是，模型训练时还需要刻意隐藏某些信息，以防分类器利用网站结构过拟合代理问题。假设用户刚刚搜索了“Taylor Swift”，如果模型知道这次搜索发生在标签观看之前，它很可能预测用户下一次观看来自该搜索结果页的视频。这样虽然可能在训练目标上表现很好，但如果系统把用户最近搜索结果页简单复现到首页推荐中，线上体验会很差。为避免这种问题，模型会丢弃部分序列信息，将搜索查询表示为无序词袋，使分类器不直接知道标签来源。

视频消费行为还具有明显的非对称性。例如，连续剧通常按顺序观看；用户发现某一音乐类型或某位艺人时，常常先从最热门内容开始，再逐渐转向更小众内容。因此，预测用户未来会观看什么，比随机留出一个历史观看记录再预测它更符合推荐场景。许多协同过滤系统通过随机隐藏用户历史中的一个项目，并用其他项目预测它，这会泄露未来信息，并忽略行为顺序中的非对称模式。本文采用“回滚”用户历史的方法：随机选择一次观看作为标签，只把该观看之前的用户行为作为输入。原文图 5 展示了随机留出观看和预测未来观看两种构造方式的差异。

**图 5：** 标签与输入上下文选择示意：预测未来观看在 A/B 测试中表现更好。

### 3.5 特征与深度实验

实验结果表明，增加特征类型和网络深度能够显著提升候选生成模型在留出数据上的精度。原文图 6 展示了不同特征组合和网络深度对平均准确率均值（MAP）的影响。在实验设置中，模型使用 100 万个视频和 100 万个搜索词元的词表，每个对象被嵌入为 256 维向量。用户最近观看和最近搜索的最大袋大小均为 50。softmax 输出层在相同的 100 万视频类别上输出多项分布，输出维度同样为 256，可以理解为另一组输出视频嵌入。

这些实验的意义在于，它们把模型深度、特征种类和推荐效果之间的关系展示出来。只使用观看历史时，模型主要依靠协同过滤信号；加入搜索历史后，模型能够捕捉用户主动表达的兴趣；加入样本年龄后，模型更好地处理视频新鲜度；加入全部特征后，模型拥有最丰富的用户和上下文描述。随着网络从线性结构变为多层结构，模型对这些特征的利用能力逐步增强。

模型在所有 YouTube 用户数据上训练至收敛，相当于对训练数据进行了多个 epoch。网络结构遵循常见的塔形设计：底层最宽，后续每个隐藏层的单元数量逐步减半。深度为零的模型基本等价于线性分解方法，其表现与前代系统相近。随着隐藏层深度增加，模型表达能力增强，能够更好地利用搜索历史、样本年龄和人口统计等附加特征。作者不断增加网络宽度和深度，直到额外收益减小并且训练收敛变得困难。

- **深度 0：** 线性层将拼接后的输入转换为 256 维 softmax 空间。
- **深度 1：** 256 个 ReLU 单元。
- **深度 2：** 512 个 ReLU 单元后接 256 个 ReLU 单元。
- **深度 3：** 1024 个 ReLU 单元后接 512 个 ReLU 单元，再接 256 个 ReLU 单元。
- **深度 4：** 2048 个 ReLU 单元后接 1024、512 和 256 个 ReLU 单元。

这些实验说明，深度本身并不是孤立地提高效果，而是让模型能够更有效地建模不同特征之间的交互。如果只有观看视频嵌入，模型类似增强版协同过滤；当加入搜索、样本年龄、人口统计和设备等特征后，深层网络可以学习“某类用户在某种上下文下对某类新视频更感兴趣”这类非线性关系。因此，特征丰富性和网络深度需要配合使用。

**图 6：** 视频嵌入之外的特征和网络深度对留出集 MAP 的提升效果。

## 4. 排序

排序阶段的主要作用，是利用曝光数据对候选生成结果进行面向具体用户界面的精细化校准。候选生成模型判断某个视频总体上可能与用户相关，但最终用户是否会在某个页面、某个缩略图、某个上下文中点击和观看，还取决于更细节的因素。例如，用户一般可能喜欢某个视频主题，但如果首页上展示的缩略图不吸引人，用户仍然可能不会点击。排序模型正是为了处理这些更接近最终展示场景的差异。

排序阶段只需要对几百个候选视频打分，因此可以使用比候选生成阶段更多、更复杂的特征。它不仅可以使用视频属性和用户属性，还可以使用用户与该视频、该频道、相似主题、历史曝光之间的关系。排序对于融合不同候选来源也非常重要。不同候选源可能有不同的生成逻辑和分数含义，无法直接比较。排序网络可以把候选来源及其分数作为特征输入，在统一目标下学习如何校准这些候选。

由于排序阶段直接决定用户看到的顺序，它对用户体验的影响非常直接。候选生成阶段只需要保证好内容进入候选集合，而排序阶段需要决定哪个内容排在第一屏、哪个内容被降到后面、哪些内容因为近期曝光但未点击而暂时不再展示。排序模型因此更接近最终产品目标，也更依赖线上 A/B 测试反馈。

本文的排序模型采用与候选生成类似的深度神经网络结构。模型通过逻辑回归为每个视频曝光独立打分，然后按照分数对视频列表排序并返回给用户。最终排序目标会根据线上 A/B 测试持续调整，但总体上是每次曝光期望观看时长的函数。作者强调，直接按点击率排序容易提升标题党或欺骗性视频，因为用户可能点击但很快退出。相比之下，观看时长更能反映用户是否真正参与和消费内容，因此是更适合 YouTube 推荐的核心目标之一。

**图 7：** 深度排序网络架构。

### 4.1 特征表示

排序模型使用的特征可以分为类别特征和连续或有序特征。类别特征的基数差异很大，有些只有两个取值，例如用户是否登录；有些则有数百万种可能取值，例如视频 ID 或用户最近一次搜索查询。类别特征还可以根据是否包含单个值或多个值进一步划分。单值类别特征的例子是当前曝光视频的 ID；多值类别特征的例子是用户最近观看过的 N 个视频 ID 集合。

特征也可以按照描述对象划分为曝光特征和查询特征。曝光特征描述当前被评分的视频或该视频与用户之间的关系，需要对每个候选视频分别计算；查询特征描述当前用户和请求上下文，每次请求只需计算一次。区分这两类特征有助于优化在线服务效率，因为排序阶段虽然候选数量较少，但仍然需要对每个候选重复计算部分特征。

#### 特征工程

尽管深度学习能够自动学习表示，但在 YouTube 排序系统中，手工特征工程仍然非常重要。原始用户行为和视频数据并不能直接以理想形式输入前馈神经网络。工程团队仍需要投入大量资源，把用户行为序列、视频属性、候选来源、曝光历史和上下文信息转换为稳定、可用且具有预测价值的特征。特别困难的是如何表示用户行为的时间序列，以及这些历史行为与当前被评分视频之间的关系。

作者观察到，最重要的排序信号往往是描述用户过去与当前项目本身或相似项目交互的特征。例如，用户过去是否观看过同一频道的视频，观看过多少个，最近一次观看该主题是什么时候，用户是否多次看到类似视频但没有点击。这类连续特征能够跨不同视频泛化，因为它们不依赖某个具体视频 ID，而是描述用户与相关内容集合之间的关系。类似经验也出现在广告点击预测系统中。

这些特征之所以有效，是因为它们比单个 ID 更能表达用户和内容之间的关系。一个新视频本身可能没有足够历史数据，但如果它来自用户经常观看的频道，或者属于用户近期持续关注的主题，排序模型仍然可以给出较高分数。相反，如果某个视频虽然全局热门，但用户已经多次看到却未点击，模型也可以根据曝光历史降低其排名。这种关系型特征是推荐系统从“内容热门度排序”走向“个性化排序”的关键。

候选生成阶段的信息也需要传递给排序阶段。例如，某个视频是由哪个候选源提名的，候选生成模型给它的分数是多少，是否来自用户最近观看视频的相似推荐，是否来自热门或订阅候选源。这些信息帮助排序模型理解候选视频为何进入候选集合，并学习不同来源在不同上下文中的可信程度。

历史曝光频率特征对于推荐列表的流动性非常关键。如果用户刚刚看到某个视频但没有点击，下一次刷新页面时继续把同一个视频排在前面往往会造成体验下降。排序模型可以利用最近曝光和观看历史降低这类视频的排名，使连续请求不会返回完全相同的列表。实时维护精确到秒级的曝光和观看历史是一项复杂工程，但对构建响应迅速的推荐系统非常重要。

#### 类别特征嵌入

与候选生成阶段类似，排序模型也使用嵌入方法把稀疏类别特征转换为稠密向量。每个唯一 ID 空间都有独立学习的嵌入表，嵌入维度大致随着唯一取值数量的对数增长。训练前系统会遍历数据构建词表。对于视频 ID、搜索词等基数极大的空间，系统只保留在被点击曝光中出现频率最高的前 N 个取值，其他词表外取值映射为零嵌入。这种截断既降低内存成本，也使模型关注高频且更可靠的类别。

多值类别特征的处理方式是先查找每个取值的嵌入，再对这些嵌入取平均后输入网络。例如，用户最近观看过的一组视频 ID 可以被表示为这些视频嵌入的平均向量。这样可以把变长集合转换为固定宽度向量。虽然这种平均会丢失顺序信息，但它在大规模系统中计算简单、稳定，并能提供有效的兴趣摘要。

一个重要设计是同一 ID 空间中的不同特征共享底层嵌入。例如，当前曝光视频 ID、用户最近观看的视频 ID、触发推荐的种子视频 ID 都属于视频 ID 空间，可以共享同一套视频嵌入。共享嵌入有助于提升泛化能力、加快训练并减少内存占用。不过，每个特征仍会分别输入上层网络，使网络能够学习不同特征角色下的专门化表示。由于高基数嵌入空间参数量巨大，共享嵌入对控制模型规模尤其重要。作者举例说，100 万个 ID 嵌入到 32 维空间的参数量，远高于宽度为 2048 的全连接层。

#### 连续特征归一化

神经网络对输入尺度和分布非常敏感。如果连续特征未经过适当归一化，不同特征的数值范围差异可能导致训练不稳定或收敛困难。与决策树集成等方法不同，神经网络通常不具备对单个特征缩放的天然不变性。因此，排序模型对连续特征进行分位数归一化。具体做法是根据特征的累积分布函数，把原始特征值映射到 [0,1) 区间，使其近似均匀分布。

由于真实累积分布无法直接解析得到，系统在训练前通过一次数据遍历计算特征分位数，再用线性插值近似该变换。除了输入归一化后的原始特征 x̃，模型还会输入 x̃² 和 √x̃。这样做可以让网络更容易构造超线性和次线性函数，提高对连续特征非线性影响的表达能力。实验表明，加入连续特征的幂次能够提升离线准确率。

例如，用户最近一次观看某频道距今的时间可能不是线性影响推荐分数：从 1 小时增加到 1 天与从 30 天增加到 31 天，对用户兴趣判断的意义并不相同。通过分位数归一化和幂次变换，网络更容易学习这种非线性关系，而不必完全依赖隐藏层从原始尺度中自行发现合适变换。这也是工业推荐系统中“深度学习仍需要特征预处理”的一个具体体现。

### 4.2 期望观看时长建模

排序模型的目标是预测某次视频曝光的期望观看时长。训练样本分为正样本和负样本：如果用户点击了曝光视频，则该样本为正，并带有实际观看时长；如果用户没有点击，则为负样本。仅预测点击概率并不充分，因为点击可能受到标题和缩略图诱导，而用户真正停留观看的时间更能反映内容价值和推荐质量。

为建模期望观看时长，作者采用加权逻辑回归。模型仍然在交叉熵损失下训练，但对正样本按照实际观看时长加权，负样本权重为 1。这样，观看时间更长的正样本在训练中具有更大影响。逻辑回归最终学习到的 odds 可以近似表示期望观看时长。若训练样本总数为 N，正样本数量为 k，第 i 个正样本观看时长为 T_i，则学习到的 odds 近似为正样本观看时长总和与负样本数量之比。在点击概率较小的情况下，这个量接近期望观看时长 E[T]。

这种训练方式把“是否点击”和“点击后看多久”合并到一个可优化目标中。未点击曝光作为负样本告诉模型哪些展示没有产生观看；被点击曝光的观看时长则告诉模型哪些点击更有价值。相比只把所有点击视为同等正样本，加权方法能够区分短暂误点和长时间观看。对于视频平台而言，这一点非常重要，因为真正有价值的推荐不仅要吸引点击，还要让用户愿意持续观看。

在线推理时，模型使用指数函数作为最终激活函数，以产生非负的 odds 输出。由于该输出近似期望观看时长，排序系统可以直接根据它对视频曝光进行排序。相比直接预测点击率，这种方法更符合 YouTube 对用户参与度的优化方向，也能减少标题党视频通过提高点击率获得过高排名的问题。

从推荐目标设计角度看，这一做法体现了 YouTube 对“点击”与“满意观看”之间差异的重视。点击率是容易获得且反馈迅速的指标，但它容易受到标题、封面和好奇心驱动影响；观看时长虽然同样是隐式反馈，却更接近用户真实投入的注意力。通过让观看时长进入训练权重，模型会把长时间观看的视频视为更强的正信号，从而把排序目标从短期点击转向更稳定的参与度。这也是工业推荐系统从简单点击优化走向多目标体验优化的重要步骤。

### 4.3 隐藏层实验

原文表 1 展示了不同隐藏层配置在次日留出数据上的结果。评估指标为加权逐用户损失。计算方式是，在同一页面中选择用户看到的正样本曝光和负样本曝光，分别用模型打分。如果负样本得分高于正样本，则认为正样本的观看时长被错误预测。最终损失等于所有预测错误观看时长占总观看时长的比例。这个指标与排序质量和观看时长目标直接相关。

实验结果显示，增加隐藏层宽度和深度都能改善排序效果。没有隐藏层时损失最高；加入 256、512 或 1024 个 ReLU 单元后，损失逐步下降；使用多层塔形结构后效果进一步改善。1024 -> 512 -> 256 的三层 ReLU 配置取得最佳结果，同时仍能满足在线服务 CPU 预算。更复杂的模型可能继续提升离线效果，但在线排序必须考虑推理成本，因此模型结构需要在效果和资源之间平衡。

表 1 中的结果还说明，排序模型同样受益于深度网络的非线性表达能力。排序阶段的特征数量多、类型复杂，许多特征之间存在交互关系。例如，用户与频道的历史、候选来源分数、近期曝光频率和设备上下文可能共同决定一次曝光是否值得靠前展示。浅层模型难以充分表达这些交互，而多层 ReLU 网络能够在可接受的计算预算内学习更复杂的组合模式。

作者还进行了两个对照实验。第一，在 1024 -> 512 -> 256 结构下，如果只输入归一化连续特征而不输入其幂次，损失会增加 0.2%，说明连续特征幂次确实提供了额外表达能力。第二，如果对正负样本赋予相同权重，而不是按观看时长加权正样本，观看时长加权损失会显著增加 4.1%。这说明直接优化点击与优化观看时长之间存在明显差异，使用观看时长加权对排序目标非常关键。

这些实验虽然以离线留出集指标呈现，但它们服务于线上系统约束。排序网络越宽越深，表达能力通常越强，但每一次用户请求都需要对数百个候选视频运行排序模型，因此推理延迟和服务器 CPU 成本会迅速上升。论文选择 1024、512、256 的塔形结构，是在效果提升与服务成本之间折中的结果。对毕业设计中的推荐系统实现而言，这一点也有启示意义：模型并非越复杂越好，必须结合数据规模、响应时间和部署资源共同考虑。

| 隐藏层配置 | 加权每用户损失 |
|---|---:|
| 无 | 41.6% |
| 256 ReLU | 36.9% |
| 512 ReLU | 36.7% |
| 1024 ReLU | 35.8% |
| 512 ReLU -> 256 ReLU | 35.2% |
| 1024 ReLU -> 512 ReLU | 34.7% |
| 1024 ReLU -> 512 ReLU -> 256 ReLU | 34.6% |

**表 1：** 更宽、更深的 ReLU 隐藏层对次日留出数据上观看时长加权成对损失的影响。

## 5. 结论

本文介绍了 YouTube 视频推荐系统中的深度神经网络架构，并将推荐任务拆分为候选生成和排序两个问题。候选生成模型负责从海量视频库中召回与用户大体相关的视频，排序模型负责结合更丰富的特征对候选视频进行精细打分。两阶段架构使系统能够在大规模语料库和严格延迟约束下同时实现可扩展性和个性化。

从整体来看，候选生成和排序并不是简单的前后两个模型，而是承担不同系统职责的两个层次。候选生成强调覆盖面、效率和粗粒度兴趣匹配；排序强调精度、上下文适配和目标函数校准。前者决定哪些内容有机会进入最终竞争，后者决定这些内容如何在界面中呈现。只有二者配合，系统才能既处理数百万视频的规模，又在用户真正看到的少量结果上保持较高质量。

深度协同过滤模型能够有效融合观看历史、搜索历史、人口统计信息、设备、地域和样本年龄等多种信号，并通过多层网络建模它们之间的非线性交互。与早期矩阵分解方法相比，深度模型在表达能力和特征融合方面具有明显优势。实验表明，增加特征和网络深度能够提升候选生成效果，但模型设计仍需要考虑收敛难度和在线服务效率。

论文强调，推荐系统中的代理学习问题选择具有很强实践性。预测未来观看比随机留出历史观看更符合真实推荐场景，因为它避免未来信息泄露，并能捕捉用户行为中的顺序性和非对称共同观看模式。同时，训练时需要有意识地屏蔽某些会导致过拟合网站结构的信息，否则模型可能在离线代理任务上表现很好，却无法迁移到首页推荐等真实场景。

这一点提醒我们，推荐模型的训练数据并不是天然客观的，它总是由产品形态和用户交互过程共同塑造。若训练样本构造方式与实际推荐场景不一致，模型可能学习到并不适合线上使用的规律。本文通过选择未来观看作为标签、回滚用户历史、限制模型可见信息等方式，使训练任务更贴近真实推荐需求。这些设计看似细节，却直接影响模型能否在生产环境中发挥作用。

样本年龄特征的引入展示了推荐系统处理非平稳内容分布的一种有效方式。视频流行度会随上传时间快速变化，模型若只学习训练窗口内平均观看概率，就会偏向旧内容。把样本年龄作为输入，可以让模型学习时间相关行为，并在在线服务时更好地推荐新上传视频。该方法既改善离线精度，也在线上实验中显著增加近期上传视频的观看时长。

这部分内容尤其适合用于理解推荐系统中的冷启动和时效性问题。新视频缺少历史交互，但并不代表它没有推荐价值；热门内容具有历史优势，但也可能已经过了最佳传播阶段。样本年龄特征提供了一种简单而有效的时间校正方式，使模型能够把上传时间纳入预测过程，而不是仅仅依赖累计观看统计。

因此，时间特征并不是附属信息，而是视频推荐任务中影响候选质量和排序效果的重要因素。

这一设计也说明，推荐系统需要持续感知内容生命周期，而不能把视频价值视为静态不变。

这对新内容推荐尤其关键。

同时也能为冷启动场景提供更可靠的建模依据。

效果更加稳定。

排序部分虽然更接近传统机器学习问题，但深度学习方法仍然优于此前的线性模型和树模型。排序模型的关键在于构造能够描述用户与视频、频道、主题、历史曝光之间关系的高质量特征。类别特征需要通过嵌入表示，连续特征需要通过分位数归一化和幂次扩展处理。多层网络能够有效建模数百个特征之间的非线性交互。

最后，论文通过加权逻辑回归把排序目标从点击概率转向期望观看时长。对正样本按观看时长加权、对负样本赋予单位权重，使模型学习到的 odds 接近期望观看时长。这种方法比直接预测点击率更符合视频推荐中对用户参与度的优化目标，也更能抑制标题党等只提升点击而不能带来持续观看的内容。整体来看，本文展示了深度学习在工业级推荐系统中的真实落地方式：模型结构、特征工程、训练目标、近似检索、在线排序和 A/B 测试必须协同设计，才能在超大规模场景中取得稳定收益。

对于本文献翻译所服务的毕业设计而言，这篇论文的价值还在于提供了推荐系统工程化实现的参考框架。它说明一个实际可用的推荐系统不仅需要算法模型，还需要数据采集、特征构造、候选召回、排序评估、在线反馈和持续迭代。深度学习提升了模型表达能力，但并没有消除特征设计、目标选择和系统约束的重要性。相反，在大规模推荐场景中，这些因素需要更加紧密地结合，才能把模型能力转化为稳定的用户体验提升。

## 6. 致谢

作者感谢 Jim McFadden 和 Pranav Khaitan 提供的重要指导与支持。Sujeet Bansal、Shripad Thite 和 Radek Vingralek 实现了训练和在线服务基础设施中的关键组件。Chris Berg 和 Trevor Walker 对本文工作进行了深入讨论，并提供了细致反馈。

## 7. 参考文献

[1] M. Abadi, A. Agarwal, P. Barham, E. Brevdo, Z. Chen, C. Citro, G. S. Corrado, A. Davis, J. Dean, M. Devin, S. Ghemawat, I. Goodfellow, A. Harp, G. Irving, M. Isard, Y. Jia, R. Jozefowicz, L. Kaiser, M. Kudlur, J. Levenberg, D. Mané, R. Monga, S. Moore, D. Murray, C. Olah, M. Schuster, J. Shlens, B. Steiner, I. Sutskever, K. Talwar, P. Tucker, V. Vanhoucke, V. Vasudevan, F. Viégas, O. Vinyals, P. Warden, M. Wattenberg, M. Wicke, Y. Yu, and X. Zheng. TensorFlow: Large-scale machine learning on heterogeneous systems, 2015. Software available from tensorflow.org.

[2] X. Amatriain. Building industrial-scale real-world recommender systems. In Proceedings of the Sixth ACM Conference on Recommender Systems, RecSys ’12, pages 7–8, New York, NY, USA, 2012. ACM.

[3] J. Davidson, B. Liebald, J. Liu, P. Nandy, T. Van Vleet, U. Gargi, S. Gupta, Y. He, M. Lambert, B. Livingston, and D. Sampath. The YouTube video recommendation system. In Proceedings of the Fourth ACM Conference on Recommender Systems, RecSys ’10, pages 293–296, New York, NY, USA, 2010. ACM.

[4] J. Dean, G. S. Corrado, R. Monga, K. Chen, M. Devin, Q. V. Le, M. Z. Mao, M. Ranzato, A. Senior, P. Tucker, K. Yang, and A. Y. Ng. Large scale distributed deep networks. In NIPS, 2012.

[5] A. M. Elkahky, Y. Song, and X. He. A multi-view deep learning approach for cross domain user modeling in recommendation systems. In Proceedings of the 24th International Conference on World Wide Web, WWW ’15, pages 278–288, New York, NY, USA, 2015. ACM.

[6] X. Glorot, A. Bordes, and Y. Bengio. Deep sparse rectifier neural networks. In G. J. Gordon and D. B. Dunson, editors, Proceedings of the Fourteenth International Conference on Artificial Intelligence and Statistics (AISTATS-11), volume 15, pages 315–323. Journal of Machine Learning Research - Workshop and Conference Proceedings, 2011.

[7] X. He, J. Pan, O. Jin, T. Xu, B. Liu, T. Xu, Y. Shi, A. Atallah, R. Herbrich, S. Bowers, and J. Q. n. Candela. Practical lessons from predicting clicks on ads at Facebook. In Proceedings of the Eighth International Workshop on Data Mining for Online Advertising, ADKDD’14, pages 5:1–5:9, New York, NY, USA, 2014. ACM.

[8] W. Huang, Z. Wu, L. Chen, P. Mitra, and C. L. Giles. A neural probabilistic model for context based citation recommendation. In AAAI, pages 2404–2410, 2015.

[9] S. Ioffe and C. Szegedy. Batch normalization: Accelerating deep network training by reducing internal covariate shift. CoRR, abs/1502.03167, 2015.

[10] S. Jean, K. Cho, R. Memisevic, and Y. Bengio. On using very large target vocabulary for neural machine translation. CoRR, abs/1412.2007, 2014.

[11] L. Jiang, Y. Miao, Y. Yang, Z. Lan, and A. G. Hauptmann. Viral video style: A closer look at viral videos on YouTube. In Proceedings of International Conference on Multimedia Retrieval, ICMR ’14, pages 193:193–193:200, New York, NY, USA, 2014. ACM.

[12] T. Liu, A. W. Moore, A. Gray, and K. Yang. An investigation of practical approximate nearest neighbor algorithms. pages 825–832. MIT Press, 2004.

[13] E. Meyerson. YouTube now: Why we focus on watch time. http://youtubecreator.blogspot.com/2012/08/youtube-now-why-we-focus-on-watch-time.html. Accessed: 2016-04-20.

[14] T. Mikolov, I. Sutskever, K. Chen, G. Corrado, and J. Dean. Distributed representations of words and phrases and their compositionality. CoRR, abs/1310.4546, 2013.

[15] F. Morin and Y. Bengio. Hierarchical probabilistic neural network language model. In AISTATS’05, pages 246–252, 2005.

[16] D. Oard and J. Kim. Implicit feedback for recommender systems. In Proceedings of the AAAI Workshop on Recommender Systems, pages 81–83, 1998.

[17] K. J. Oh, W. J. Lee, C. G. Lim, and H. J. Choi. Personalized news recommendation using classified keywords to capture user preference. In 16th International Conference on Advanced Communication Technology, pages 1283–1287, Feb 2014.

[18] S. Sedhain, A. K. Menon, S. Sanner, and L. Xie. AutoRec: Autoencoders meet collaborative filtering. In Proceedings of the 24th International Conference on World Wide Web, WWW ’15 Companion, pages 111–112, New York, NY, USA, 2015. ACM.

[19] X. Su and T. M. Khoshgoftaar. A survey of collaborative filtering techniques. Advances in Artificial Intelligence, 2009:4, 2009.

[20] D. Tang, B. Qin, T. Liu, and Y. Yang. User modeling with neural network for review rating prediction. In Proc. IJCAI, pages 1340–1346, 2015.

[21] A. van den Oord, S. Dieleman, and B. Schrauwen. Deep content-based music recommendation. In C. J. C. Burges, L. Bottou, M. Welling, Z. Ghahramani, and K. Q. Weinberger, editors, Advances in Neural Information Processing Systems 26, pages 2643–2651. Curran Associates, Inc., 2013.

[22] H. Wang, N. Wang, and D.-Y. Yeung. Collaborative deep learning for recommender systems. In Proceedings of the 21th ACM SIGKDD International Conference on Knowledge Discovery and Data Mining, KDD ’15, pages 1235–1244, New York, NY, USA, 2015. ACM.

[23] J. Weston, S. Bengio, and N. Usunier. WSABIE: Scaling up to large vocabulary image annotation. In Proceedings of the International Joint Conference on Artificial Intelligence, IJCAI, 2011.

[24] J. Weston, A. Makadia, and H. Yee. Label partitioning for sublinear ranking. In S. Dasgupta and D. Mcallester, editors, Proceedings of the 30th International Conference on Machine Learning (ICML-13), volume 28, pages 181–189. JMLR Workshop and Conference Proceedings, May 2013.

[25] X. Yi, L. Hong, E. Zhong, N. N. Liu, and S. Rajan. Beyond clicks: Dwell time for personalization. In Proceedings of the 8th ACM Conference on Recommender Systems, RecSys ’14, pages 113–120, New York, NY, USA, 2014. ACM.

[26] Zayn. Pillowtalk. https://www.youtube.com/watch?v=C_3d6GntKbk.
